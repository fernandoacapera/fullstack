/*
const nome = {
    texto: "fe",
    length: 2,
    charAt(){}
}
*/

let nome = 'Fernando'
// nome.length -> contar a quantidade de caracteres

/* nome.charAt() -> ir em algum caracter passando o valor da sua posição.
Exemplo: carro (c=0, a=1, r=2, r=3, o=4)
*/

// nome.replace("Fernando", "Fernando Capera") -> substitiu. primeiro parametro é o que eu quero localizar e o seguindo é o que vai substituir

// nome.substr(0,4) -> retorna da posicao zero até a posição 4. É possivel concatenar, tipo: nome.substr(0,4) + "..." ficando: Ferna...

//nome.split("-") -> se eu tiver uma string como "fernando-capera", ele separara como [fernando,capera]. por conta de eu colocar - no .split

// nome.slice(0,4) -> faz a mesma coisa que o substr

// nome.toUpperCase() -> nome maisculo

// nome.toLowerCase() -> nome minusculo

//nome.trim() -> remove o excesso de espaço. tipo: "Fernando            "
